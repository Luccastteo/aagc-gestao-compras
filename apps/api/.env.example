# ========== DATABASE ==========
DATABASE_URL="postgresql://aagc:aagc_dev_password@localhost:5432/aagc_db?schema=public"
REDIS_URL="redis://localhost:6379"

# ========== API ==========
PORT=3001
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-change-in-production
FRONTEND_URL=http://localhost:3000
# Origens permitidas no CORS (separadas por vírgula). Ex: http://localhost:3000,http://localhost:1420
CORS_ORIGINS=http://localhost:3000

# ========== RATE LIMITING ==========
# TTL em segundos, MAX requisições por TTL
# DEV sugerido: 60s / 120 req
# PROD sugerido: 60s / 60 req
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=120

# ========== PASSWORD POLICY ==========
# Comprimento mínimo de senha (padrão: 10)
# Sempre requer: maiúscula, minúscula, número, símbolo e não pode ser senha comum
PASSWORD_MIN_LENGTH=10

# ========== EMAIL (SMTP) ==========
# Configure para enviar emails reais
# Exemplo com Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587
# Exemplo com Outlook: SMTP_HOST=smtp-mail.outlook.com, SMTP_PORT=587
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# ========== TWILIO (WhatsApp e SMS) ==========
# Crie uma conta em https://www.twilio.com
# WhatsApp requer configuração do Sandbox ou API oficial
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=+14155238886
TWILIO_SMS_FROM=

# ========== AI CONFIGURATION ==========
# OpenAI API Key para LLM e embeddings
OPENAI_API_KEY=sk-your-openai-api-key-here
# Modelo OpenAI (gpt-4-turbo-preview, gpt-4, gpt-3.5-turbo)
OPENAI_MODEL=gpt-4-turbo-preview
# URL do serviço ML (FastAPI)
ML_SERVICE_URL=http://localhost:8001

# ========== AI FEATURES ==========
# Habilitar aprovação automática de PO baseada em AI
ENABLE_AUTO_APPROVAL=true
# Habilitar compra automática inteligente
ENABLE_AUTO_PURCHASE=true


